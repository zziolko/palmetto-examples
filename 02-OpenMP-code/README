1. Load modules needed for compilation

module load gcc/4.8.1

2. Compile the code with OpenMP 

gcc -fopenmp hello_world_openmp.c -o hello.x

3. Prepare/Verify the submit script

4. Submit the job 

qsub job.sh


